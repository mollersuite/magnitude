import type { LocalStorage } from "@raycast/api"

export default (extensionName: string): typeof LocalStorage => ({
	async getItem (key) {
		
	},
	async setItem (key, value) {
		
	},
	async allItems () {
		
	},
	async removeItem (key) {
		
	},
	async clear () {
		
	}
})